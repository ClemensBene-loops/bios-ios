<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleDisplayName</key>
	<string>BIOS</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UIApplicationSceneManifest</key>
	<dict>
		<key>UIApplicationSupportsMultipleScenes</key>
		<false/>
	</dict>
	<key>UILaunchScreen</key>
	<dict/>
	<!-- v2 is dark first (approved mockup): fixed app-wide from launch, also for
	     system sheets and the launch screen, no light flash. -->
	<key>UIUserInterfaceStyle</key>
	<string>Dark</string>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>
	<!-- BIOS server config. Empty in git; the build workflow fills them from the
	     GitHub secrets BIOS_API_BASE_URL / BIOS_API_SECRET (plutil, CI only).
	     Read in code via AppConfig. -->
	<key>BIOSAPIBaseURL</key>
	<string></string>
	<key>BIOSAPISecret</key>
	<string></string>
	<!-- Same value as the aps-environment entitlement (xcconfig APS_ENVIRONMENT):
	     development (Debug) or production (Release/TestFlight). The app sends it
	     to the server so the right APNs host (sandbox/production) is used. -->
	<key>BIOSAPSEnvironment</key>
	<string>$(APS_ENVIRONMENT)</string>
</dict>
</plist>
